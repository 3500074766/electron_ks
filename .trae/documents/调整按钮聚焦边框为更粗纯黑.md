修改 DataTable 按钮的聚焦样式为更粗的纯黑焦点环：
- 在 `src/renderer/src/components/DataTable.vue` 的样式中，将当前 `box-shadow: 0 0 0 2px rgba(0, 0, 0, 0.35)` 修改为 `box-shadow: 0 0 0 3px #000`，保留 `outline: none`。
验证：Tab 聚焦或点击后按钮出现更粗的黑色焦点环；其它交互不受影响。