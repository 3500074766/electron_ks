- 仅对滚轮生效：将 `onWheelRoi` 的最小值从 0 改为 2；当计算结果 <2 时不再下降（保持为 2）。
- 首次滚轮初始化：输入为空时，用实时 ROI 初始化；若实时值 <2，则初始化为 2。
- 手动输入与提交逻辑保持不变，可输入任意值（含 <2）。
- 保留 Ctrl=0.1、Shift=5 步长与两位小数格式。

文件：`src/renderer/src/components/DataTable.vue` 更新 `onWheelRoi` 下限判断。