实现点：
1) ROI 滚轮：
- 每次滚动以输入框当前值为基础增减；输入为空时先用实时 ROI 初始化为至少 1。
- 下限为 1，继续向下滚动不再生效。

2) 修改提交：
- 若 newRoi < 1：直接不提交、不提示。
- 若 newRoi - 实时ROI > 20：弹出确认框（确认/取消），确认后才发送更新请求；取消不提交。

改动文件：
- DataTable.vue：
  - 更新 onWheelRoi 限制值下限为 1；首次滚动赋初值为 max(1, 实时ROI)。
  - 将 handleUpdateRoi 改为含确认逻辑；引入 ElMessageBox。

验证：
- 向下滚动到 1 后继续滚动无效果。
- 大幅上调（>20）会弹确认；确认提交，取消不提交。
- 提交成功后输入框清空（保留现有逻辑）。