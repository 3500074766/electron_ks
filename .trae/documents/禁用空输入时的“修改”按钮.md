更新两点：
1) 按钮焦点颜色：将焦点时的蓝色改为偏黑的焦点环，兼顾可见度。
- 样式：`:deep(.el-button:focus), :deep(.el-button:focus-visible) { outline:none; box-shadow:0 0 0 2px rgba(0,0,0,0.35); }`

2) 空输入禁用提交：输入框为空时“修改”按钮禁用；回车也不提交。
- 模板为按钮加 `:disabled="isEmptyRoi(scope.row)"`
- 新增 `isEmptyRoi(row)` 与 `onEnterSubmit(row)`，回车调用该函数以校验。

文件：`src/renderer/src/components/DataTable.vue`

验证：
- 按 Tab 焦点按钮出现偏黑焦点环；
- 输入为空时按钮禁用且回车不提交；
- 其他逻辑（滚轮步长、两位小数、阈值确认）保持。