- 将 ROI 输入框恢复为默认尺寸（去掉 size 属性）。
- 将“修改”按钮设置为较大尺寸：`size="large"`，以获得更高的按钮高度。
- 不更改宽度与交互逻辑，保持回车提交、禁用空输入、焦点样式等现有行为。
- 文件：`src/renderer/src/components/DataTable.vue` 模板中的 ROI 输入与按钮。
- 验证：按钮高度明显大于当前，输入框保持原高度；行内对齐正常。