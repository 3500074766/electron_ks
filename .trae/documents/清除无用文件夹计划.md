# 清除无用文件夹计划

## 一、检查结果
通过对项目结构的详细检查，发现以下文件夹已变为无用或空文件夹：

1. **后端文件夹**：
   - `src/main/ipc` - 已删除所有文件
   - `src/main/services` - 已删除所有文件
   - `src/main/utils` - 已删除所有文件
   - `src/main/db` - 已删除所有文件

2. **前端文件夹**：
   - `src/renderer/src/layouts` - 已删除所有文件
   - `src/renderer/src/components` - 空文件夹
   - `src/renderer/src/assets` - 空文件夹
   - `src/renderer/src/stores` - 空文件夹（已创建新的`store`文件夹）

## 二、执行步骤

1. 删除后端无用文件夹：
   - `src/main/ipc`
   - `src/main/services`
   - `src/main/utils`
   - `src/main/db`

2. 删除前端无用文件夹：
   - `src/renderer/src/layouts`
   - `src/renderer/src/components`
   - `src/renderer/src/assets`
   - `src/renderer/src/stores`

## 三、预期结果

- 项目结构更加简洁，只保留必要的文件夹
- 消除无用的空文件夹，减少文件系统混乱
- 符合极简主义重构的目标

该计划将进一步优化项目结构，使代码组织更加清晰，便于后续开发和维护。