- 将表头背景改为 Element Plus 主题的浅蓝色，保持统一风格。
- 使用主题变量：`var(--el-color-primary-light-9)`，必要时增加底边框 `var(--el-color-primary-light-7)`。
- 文件：`src/renderer/src/components/DataTable.vue` 的样式规则。
- 验证：表头呈浅蓝背景、对比清晰，滚动不影响样式。