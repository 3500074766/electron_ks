修改 DataTable 组件样式，为表格标题行添加统一背景色：
- 在 `src/renderer/src/components/DataTable.vue` 的 scoped 样式里新增规则：
  - `:deep(.el-table__header-wrapper .el-table__header th) { background-color: #f5f7fa !important; }`
- 可根据需要更换为你喜欢的颜色（例如更深的灰或主题色）。
验证：表格标题行显示指定背景色，滚动时保持不变。