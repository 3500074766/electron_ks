## 目标
- 恢复到“表格头不动、表格体滚动”的早期表现。

## 变更点
1) DataTable.vue
- 去掉外层 `table-wrapper`，直接使用 `ElTable`。
- 设置 `height="65vh"`（或你之前使用的固定值），让 Header 固定、Body 滚动。
- 保留 `style="width:100%"`，移除 `height:100%` 与父级 flex 依赖。
- 移除为卡片 Body 添加的 `flex` 伸展样式：
  - 删除 `.data-card { height:100%; display:flex; ... }`
  - 删除 `:deep(.data-card .el-card__body) { flex:1; ... }`
  - 删除 `.table-wrapper { ... }`

2) MainView.vue 
- 保持现状，不强制 DataTable 伸展；如需要，我会同时去掉 `style="flex:1"`（仅在表现异常时）。

## 验证
- 表格头部固定不滚动；表格体垂直滚动。
- 页面其他区域不受影响；数据正常展示。

如你确认，我将按以上内容回退并立即验证。