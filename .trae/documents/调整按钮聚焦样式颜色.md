目标：把“修改”按钮获得焦点时的橙色轮廓改为与 Element Plus 主题一致的蓝色，并保留无障碍可见的焦点样式。

修改：
- 在 `src/renderer/src/components/DataTable.vue` 的样式内新增聚焦样式：
  - `:deep(.el-button:focus), :deep(.el-button:focus-visible) { outline: none; box-shadow: 0 0 0 2px rgba(64,158,255,0.45); }`
- 不影响按钮的正常 hover/active 样式，仍保持高对比可见焦点环。

验证：
- 键盘 Tab 聚焦“修改”按钮时显示柔和蓝色焦点环；点击也不出现橙色轮廓。