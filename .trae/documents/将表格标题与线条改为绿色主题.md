- 使用 Element Plus 的成功色（绿色）变量替换当前的蓝色主题。
- 表头背景：改为 `var(--el-color-success-light-9)`，底边框改为 `var(--el-color-success-light-7)`。
- 表格边框与分隔线：将 `--el-table-border-color` 和各单元格/表头的 `border-color` 改为 `var(--el-color-success-light-7)`。
- 文件：`src/renderer/src/components/DataTable.vue` 样式。
- 结果：表格标题与线条呈绿色风格，与成功色保持一致。